import math
import sys

stringRadius = input('Enter a number: ')
print (stringRadius)

piValue = math.pi
intRadius = float(stringRadius)

areaCircle = piValue * math.pow(intRadius, 2)
print (areaCircle)


