# Question 6
# Get the command-line arguments (name of the script, the number of arguments, arguments) passed to a script
	
import sys

print('Number of arguments:', len(sys.argv))
print('Arguments / name of the script:', str(sys.argv))
