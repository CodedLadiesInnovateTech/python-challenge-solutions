# Question 4
# Hash a word

# Most of the time a hash function will produce unique output for a given input.
# It takes input of a variable length sequence of bytes and converts it to a fixed length sequence.

word = input("Enter a word: ")
print(hash(word))
